apiVersion: v1
kind: ConfigMap
metadata:
  name: pay-backend-html
  namespace: dev
data:
  index.html: |
    <html>
    <body>
    <h1>Payment Backend Service</h1>
    <p>This is Payment backend pod responding.</p>
    </body>
    </html>
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pay
  namespace: dev
  labels:
    app: payment
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment
      tier: backend
  template:
    metadata:
      labels:
        app: payment
        tier: backend
    spec:
      containers:
        - name: nginx
          image: nginx:alpine-slim
          ports:
            - containerPort: 80
          volumeMounts:
            - name: html
              mountPath: /usr/share/nginx/html
      volumes:
        - name: html
          configMap:
            name: pay-backend-html
---
apiVersion: v1
kind: Service
metadata:
  name: pay-svc
  namespace: dev
spec:
  selector:
    app: payment
    tier: backend
  ports:
    - port: 80
      targetPort: 8080 
      protocol: TCP
  type: ClusterIP
